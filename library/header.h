#ifndef HEADER_H
#define HEADER_H

#include <string>
#include <tuple>
#include <vector>
#include <algorithm>
#include <iostream>
using std::get;
using std::sort;
using std::endl;
using std::cin;
using std::cout;
using std::exception;
using std::string;
using std::vector;
using std::tuple;

using date_t = tuple<size_t, size_t, size_t>;
using fullname_t = tuple<string, string, string>;
#endif
